name: Deploy Function App

on:
  push:
    branches: [main]
  workflow_dispatch:

env:
  AZURE_FUNCTIONAPP_NAME: func-ordermanager-nilambur
  DOTNET_VERSION: '10.0.x'

permissions:
  id-token: write
  contents: read

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Setup .NET
        uses: actions/setup-dotnet@v4
        with:
          dotnet-version: ${{ env.DOTNET_VERSION }}

      - name: Run tests
        run: dotnet test tests/OrderManager.Backend.Tests/OrderManager.Backend.Tests.csproj --configuration Release

      # Target the Function App project explicitly. A bare `dotnet publish` here would
      # resolve to whatever project/solution sits at the repo root, which can drag the
      # test project (and xunit) into the deployed package.
      - name: Build and publish
        run: dotnet publish ordermanager-backend.csproj --configuration Release --output ./publish-output

      - name: Azure login (OIDC)
        uses: azure/login@v2
        with:
          client-id: ${{ secrets.AZURE_CLIENT_ID }}
          tenant-id: ${{ secrets.AZURE_TENANT_ID }}
          subscription-id: ${{ secrets.AZURE_SUBSCRIPTION_ID }}

      - name: Deploy to Azure Functions
        uses: Azure/functions-action@v1
        with:
          app-name: ${{ env.AZURE_FUNCTIONAPP_NAME }}
          package: ./publish-output
